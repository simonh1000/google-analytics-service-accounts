{
  "name": "ga-service-acct",
  "version": "0.1",
  "description": "Node interface to get data from Google Analytics REST service",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Simon Hampton",
  "license": "ISC",
  "dependencies": {
    "googleapis": "^1.1.5",
    "jsonwebtoken": "^4.0.0",
    "request": "^2.53.0"
  }
}
